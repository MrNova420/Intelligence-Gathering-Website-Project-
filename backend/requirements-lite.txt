# Lightweight Requirements - SQLite Only (No PostgreSQL dependencies)
# For Termux, Android, and local development environments

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic[email]==2.5.0
python-multipart==0.0.6
requests==2.31.0
python-dotenv==1.0.0

# Authentication & Security (optional dependencies handled gracefully)
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
cryptography>=41.0.7
bcrypt==4.1.2
pyjwt==2.8.0
pydantic-settings==2.1.0

# Scanner dependencies
dnspython==2.4.2
phonenumbers==8.13.26
pyotp==2.9.0
email-validator==2.1.0

# Optional dependencies for enhanced features
pillow==10.1.0
qrcode[pil]==7.4.2
reportlab==4.0.7
python-Levenshtein==0.25.0
textdistance==4.6.1

# HTTP client
aiohttp==3.9.1

# Redis (optional - will use fallback if not available)
redis==5.0.1

# System monitoring and testing dependencies
psutil==5.9.6
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1

# Note: psycopg2-binary, alembic, celery, and stripe are excluded
# for lightweight installations. Add them back if PostgreSQL/production features needed.